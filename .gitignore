__pycache__/
*.py[cod]
.venv*/
node_modules/
frontend/out/
frontend/.next/
.dev-data/
*.xlsx
!tests/fixtures/**/*.xlsx
# The bundle is vendored verbatim. A workbook it ships is authority,
# and its own manifest hashes it -- an ignore rule must not filter it.
!vendor/**
.DS_Store
.env
bandit.json
uv.lock
